const {assert} = require('chai');

// UNIT TESTING FÖR CART

describe('Cart', () => {

    describe('', () => {

        it('', () => {
            assert.throw( () => {

                })
            })
        })
    })